
// System and Qt Includes


// WMS Manager Includes
#include "CwmsJson.h"

// own Includes
//#include "CwhWms.h"


#include "CwmscomJniInterface.h"

CwmscomJniInterface::CwmscomJniInterface()
{
    //CwhWms cHandlerCreator;
    //CwmscomHandler* pHandler = cHandlerCreator.CreateHandler();
    /*
    IwmsParser* pParser = new CwmsJson();
    IwmsSerializer* pSerializer =  new CwmsJson();


    if (pHandler && pParser && pSerializer)
    {
       pHandler->SetParser(pParser); // change of ownership
       pHandler->SetSerializer(pSerializer); // change of ownership
    CwhWms cHandlerCreator;
    CwmscomHandler* pHandler = cHandlerCreator.CreateHandler();
    IwmsParser* pParser = new CwmsJson();
    IwmsSerializer* pSerializer =  new CwmsJson();

    if (pHandler && pParser && pSerializer)
    {
       pHandler->SetParser(pParser); // change of ownership
       pHandler->SetSerializer(pSerializer); // change of ownership
    }
    */
}
